bundle:
  name: intelligent-darts-joe

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build && python3 scripts/patch_app_yml.py

resources:
  apps:
    intelligent-darts-app:
      name: "intelligent-darts-joe"
      description: "intelligent-darts created with apx"
      source_code_path: ./.build
      resources:
        - name: "serving-endpoint"
          description: "Dart pose detection model serving endpoint"
          serving_endpoint:
            name: "dart_pose_detector"
            permission: "CAN_QUERY"

targets:
  dev:
    mode: development
    default: true
